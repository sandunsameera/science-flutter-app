class Reservation {
  String date, route;
  double latitude;
  double longitude;
  String username;
  String time;

  Reservation(this.date, this.route, this.username, this.time, this.latitude,
      this.longitude);
}
